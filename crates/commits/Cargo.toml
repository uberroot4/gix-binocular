[package]
name = "commits"
version = "0.1.0"
edition = "2021"
autotests = false
include = ["src/**/*"]

[lib]
doctest = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
gix = { workspace = true, features = ["max-performance-safe", "mailmap"] }
log = { workspace = true }
anyhow = { workspace = true }
render = { path = "../render" }
shared = { path = "../shared" }
base64 = "0.22.1"